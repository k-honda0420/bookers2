
<div class="container">
  <div class="row">
    
   <div class="col-md-3" >
     <h1>User info</h1>
        <%= render 'users/show', user: @new_user %>
        <%= render 'books/book', book: @book_new %>
    </div>
    
    
<div class="col-md-8 offset-md-1">  
<h1>Users</h1>
<table class="table table-striped">
  <tr>
  　<th>image</th>
    <th>name</th>
    <th></th>
  </tr>
<% @user.each do |user| %>
<tr>
<td><%= attachment_image_tag user, :profile_image, :fill, 30, 30, format: 'jpeg', fallback: "no_image.jpg" %></td>
<td><%= user.name %></td>
<td><%= link_to "Show", user_path(user) %></td>


<% end %>

 </table>